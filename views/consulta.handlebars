<div class="container text-center bg-body-tertiary">
    <div class="row">
        <div class="col-1">
            &nbsp;
        </div>
        <div class="col-1">
            &nbsp;
        </div>
    </div>
    <div class="row">
        <div class="col-1">
            &nbsp;
        </div>
        <div>
            &nbsp;
        </div>
        <div class="col-1">
            &nbsp;
        </div>
    </div>
    <div class="row">
        <div class="col-1">
            &nbsp;
        </div>
    <div class="col bg-white">
            <table class="rounded table table-striped-columns">
                <thead>
                    <tr>
                        <th scope="col">Produto</th>
                        <th scope="col">Marca</th>
                        <th scope="col">Modelo </th>
                        <th scope="col">Descrição</th>
                        <th scope="col">Palavras Chaves</th>
                        <th scope="col">Ações</th>
                    </tr>
                </thead>
                <tbody >
                {{#each post}}
                    {{#dataValues}}
                    <tr>
                        <td>{{produto}}</td>
                        <td>{{marca}}</td>
                        <td>{{modelo}}</td>
                        <td>{{descricao}}</td>
                        <td>{{palavraschaves}}</td>
                        <td>
                            <button type="button" class="btn btn-dark"><a href="/editar/{{id}}" style="color:inherit" style="margin-bottom: 0.5rem">Editar</a></button>
                            <button type="button" class="btn btn-dark"><a href="/excluir/{{id}}" style="color:inherit">Excluir</a></button>
                        </td>
                    </tr>
                    {{/dataValues}}
                {{/each}}
                </tbody>
            </table>
            </p>
        </div>
    </div>
</div>
</div>